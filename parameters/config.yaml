# This example doesn't use variables, but parameters directory is required
# by the framework structure

placeholder:
  note: "This example demonstrates basic sequential execution without variable interpolation"
